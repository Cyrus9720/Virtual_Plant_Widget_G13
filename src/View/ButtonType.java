package View;

/**
 * The ButtonType enum represents the types of buttons in the user interface.
 * This enum defines different types of buttons that can be used in the application,
 * such as buttons for watering plants.
 * Currently, it only contains one type: {@code Water}.
 * @author annagranberg
 */
public enum ButtonType {
    Water,
    NightMode,
}
